import React from "react";
import Layout from "../layouts/es";

const NotFoundPage = () => (
  <Layout>
    <div>
      <h1>No disponible</h1>
      <p>Usted sigui&accuteo; una ruta que no existe...</p>
    </div>
  </Layout>
);

export default NotFoundPage;
